trigger:
  branches:
    include:
      - main

variables:
  terraformVersion: '1.7.0'
  azureServiceConnection: 'devops-connection'
  agentPool: 'epicbook-agent'
  workingDir: '$(System.DefaultWorkingDirectory)'

stages:

# ─────────────────────────────────────────────────────────────────────────────
# STAGE 1 — INITIALIZE ENVIRONMENT
# ─────────────────────────────────────────────────────────────────────────────
- stage: TerraformInit
  displayName: 'Stage 1 — Terraform Init'
  jobs:
  - job: init
    pool:
      name: $(agentPool)
    steps:
    - checkout: self

    - script: |
        sudo apt-get update && sudo apt-get install -y unzip curl
        if ! command -v az &> /dev/null; then
          curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash
        fi
        wget -q https://releases.hashicorp.com/terraform/$(terraformVersion)/terraform_$(terraformVersion)_linux_amd64.zip
        unzip -o terraform_$(terraformVersion)_linux_amd64.zip
        sudo mv terraform /usr/local/bin/terraform
      displayName: 'Install Dependencies & Terraform'

    - task: AzureCLI@2
      name: extractCreds
      inputs:
        azureSubscription: $(azureServiceConnection)
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        addSpnToEnvironment: true
        inlineScript: |
          echo "##vso[task.setvariable variable=ARM_CLIENT_ID]$servicePrincipalId"
          echo "##vso[task.setvariable variable=ARM_CLIENT_SECRET;isSecret=true]$servicePrincipalKey"
          echo "##vso[task.setvariable variable=ARM_TENANT_ID]$tenantId"
          echo "##vso[task.setvariable variable=ARM_SUBSCRIPTION_ID]$(az account show --query id -o tsv)"

    - script: |
        cd $(workingDir)
        terraform init
      displayName: 'Terraform Init'
      env:
        ARM_CLIENT_ID: $(ARM_CLIENT_ID)
        ARM_CLIENT_SECRET: $(ARM_CLIENT_SECRET)
        ARM_TENANT_ID: $(ARM_TENANT_ID)
        ARM_SUBSCRIPTION_ID: $(ARM_SUBSCRIPTION_ID)

# ─────────────────────────────────────────────────────────────────────────────
# STAGE 2 — GENERATE PLAN
# ─────────────────────────────────────────────────────────────────────────────
- stage: TerraformPlan
  displayName: 'Stage 2 — Terraform Plan'
  dependsOn: TerraformInit
  jobs:
  - job: plan
    pool:
      name: $(agentPool)
    steps:
    - checkout: self

    - task: AzureCLI@2
      name: extractCreds
      inputs:
        azureSubscription: $(azureServiceConnection)
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        addSpnToEnvironment: true
        inlineScript: |
          echo "##vso[task.setvariable variable=ARM_CLIENT_ID]$servicePrincipalId"
          echo "##vso[task.setvariable variable=ARM_CLIENT_SECRET;isSecret=true]$servicePrincipalKey"
          echo "##vso[task.setvariable variable=ARM_TENANT_ID]$tenantId"
          echo "##vso[task.setvariable variable=ARM_SUBSCRIPTION_ID]$(az account show --query id -o tsv)"

    - task: DownloadSecureFile@1
      name: sshPublicKey
      inputs:
        secureFile: 'id_rsa.pub'

    - script: |
        cd $(workingDir)
        terraform init
        terraform plan \
          -var="public_key=$(sshPublicKey.secureFilePath)" \
          -var="db_admin_password=$(DB_ADMIN_PASSWORD)" \
          -out=tfplan
      displayName: 'Terraform Plan'
      env:
        ARM_CLIENT_ID: $(ARM_CLIENT_ID)
        ARM_CLIENT_SECRET: $(ARM_CLIENT_SECRET)
        ARM_TENANT_ID: $(ARM_TENANT_ID)
        ARM_SUBSCRIPTION_ID: $(ARM_SUBSCRIPTION_ID)
        DB_ADMIN_PASSWORD: $(DB_ADMIN_PASSWORD)

    - publish: $(workingDir)/tfplan
      artifact: tfplan

# ─────────────────────────────────────────────────────────────────────────────
# STAGE 3 — APPLY CHANGES
# ─────────────────────────────────────────────────────────────────────────────
- stage: TerraformApply
  displayName: 'Stage 3 — Terraform Apply'
  dependsOn: TerraformPlan
  jobs:
  - job: apply
    pool:
      name: $(agentPool)
    steps:
    - checkout: self
    - download: current
      artifact: tfplan

    - task: AzureCLI@2
      name: extractCreds
      inputs:
        azureSubscription: $(azureServiceConnection)
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        addSpnToEnvironment: true
        inlineScript: |
          echo "##vso[task.setvariable variable=ARM_CLIENT_ID]$servicePrincipalId"
          echo "##vso[task.setvariable variable=ARM_CLIENT_SECRET;isSecret=true]$servicePrincipalKey"
          echo "##vso[task.setvariable variable=ARM_TENANT_ID]$tenantId"
          echo "##vso[task.setvariable variable=ARM_SUBSCRIPTION_ID]$(az account show --query id -o tsv)"

    - task: DownloadSecureFile@1
      name: sshPrivateKey
      inputs:
        secureFile: 'id_rsa'

    - task: DownloadSecureFile@1
      name: sshPublicKey
      inputs:
        secureFile: 'id_rsa.pub'

    - script: |
        mkdir -p ~/.ssh
        cp $(sshPrivateKey.secureFilePath) ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        cp $(sshPublicKey.secureFilePath) ~/.ssh/id_rsa.pub
        chmod 644 ~/.ssh/id_rsa.pub
      displayName: 'Install SSH Keys'

    - script: |
        cd $(workingDir)
        terraform init
        terraform apply -auto-approve $(Pipeline.Workspace)/tfplan/tfplan
      displayName: 'Terraform Apply'
      env:
        ARM_CLIENT_ID: $(ARM_CLIENT_ID)
        ARM_CLIENT_SECRET: $(ARM_CLIENT_SECRET)
        ARM_TENANT_ID: $(ARM_TENANT_ID)
        ARM_SUBSCRIPTION_ID: $(ARM_SUBSCRIPTION_ID)

# ─────────────────────────────────────────────────────────────────────────────
# STAGE 4 — OUTPUTS
# ─────────────────────────────────────────────────────────────────────────────
- stage: PublishOutputs
  displayName: 'Stage 4 — Publish Infrastructure Outputs'
  dependsOn: TerraformApply
  jobs:
  - job: outputs
    pool:
      name: $(agentPool)
    steps:
    - checkout: self

    # 1. Standard credential extraction (Required to talk to the state file)
    - task: AzureCLI@2
      name: extractCreds
      inputs:
        azureSubscription: $(azureServiceConnection)
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        addSpnToEnvironment: true
        inlineScript: |
          echo "##vso[task.setvariable variable=ARM_CLIENT_ID]$servicePrincipalId"
          echo "##vso[task.setvariable variable=ARM_CLIENT_SECRET;isSecret=true]$servicePrincipalKey"
          echo "##vso[task.setvariable variable=ARM_TENANT_ID]$tenantId"
          echo "##vso[task.setvariable variable=ARM_SUBSCRIPTION_ID]$(az account show --query id -o tsv)"

    # 2. Extract and Log Outputs
    - script: |
        cd $(workingDir)
        terraform init # Required to access the backend state
        
        # Capture outputs from Terraform
        VM_IP=$(terraform output -raw vm_public_ip)
        DB_FQDN=$(terraform output -raw mysql_fqdn)

        # Set them as Pipeline Variables for use in other tasks
        echo "##vso[task.setvariable variable=VM_PUBLIC_IP;isOutput=true]$VM_IP"
        echo "##vso[task.setvariable variable=MYSQL_FQDN;isOutput=true]$DB_FQDN"

        # Print to the console for easy manual copying
        echo "========================================================="
        echo "INFRASTRUCTURE DEPLOYMENT COMPLETE"
        echo "========================================================="
        echo "Web VM Public IP: $VM_IP"
        echo "MySQL Server FQDN: $DB_FQDN"
        echo "========================================================="
      displayName: 'Extract and Output IP and FQDN'
      name: infraData
      env:
        ARM_CLIENT_ID: $(ARM_CLIENT_ID)
        ARM_CLIENT_SECRET: $(ARM_CLIENT_SECRET)
        ARM_TENANT_ID: $(ARM_TENANT_ID)
        ARM_SUBSCRIPTION_ID: $(ARM_SUBSCRIPTION_ID)